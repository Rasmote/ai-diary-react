# ai-diary-react
ai-diary's frontend

1. return 안에 기본 html 뼈대 작성
2. .useState()를 활용하여 기본 변수 설정해두기
3. UI와 State를 value, onChange{(e)=> set변수명(e.target.vlaue)} 등으로 연결
4. 버튼을 클릭했을때 실행할 함수를 만들기
5. 4번 함수 안에서 서버를 호출하기